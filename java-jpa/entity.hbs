// Auto-generated by Daxonne — do not edit manually

import jakarta.persistence.*;
import java.io.Serializable;

@Entity
@Table(name = "{{name}}")
public class {{PascalCase name}} implements Serializable {

{{#each columns}}
{{#if isPrimary}}
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
{{/if}}
    @Column(name = "{{name}}"{{#unless nullable}}, nullable = false{{/unless}})
    private {{JavaType type}} {{CamelCase name}};

{{/each}}
    public {{PascalCase name}}() {}

{{#each columns}}
    public {{JavaType type}} get{{PascalCase name}}() { return {{CamelCase name}}; }
    public void set{{PascalCase name}}({{JavaType type}} {{CamelCase name}}) { this.{{CamelCase name}} = {{CamelCase name}}; }

{{/each}}
}
