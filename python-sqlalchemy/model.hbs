# Auto-generated by Daxonne — do not edit manually

from sqlalchemy import Column, ForeignKey{{#each columns}}, {{SQLAlchemyType type}}{{/each}}
from sqlalchemy.orm import DeclarativeBase, relationship


class Base(DeclarativeBase):
    pass


class {{PascalCase name}}(Base):
    __tablename__ = "{{name}}"

{{#each columns}}
    {{SnakeCase name}} = Column({{SQLAlchemyType type}}{{#unless nullable}}, nullable=False{{/unless}}{{#if isPrimary}}, primary_key=True{{/if}})
{{/each}}

    def __repr__(self) -> str:
        return f"<{{PascalCase name}} {{PrimaryKeyCamelCase columns}}={self.{{PrimaryKeyCamelCase columns}}}>"
