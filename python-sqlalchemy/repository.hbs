# Auto-generated by Daxonne — do not edit manually

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from .{{SnakeCase name}} import {{PascalCase name}}


class {{PascalCase name}}Repository:
    def __init__(self, session: AsyncSession) -> None:
        self._session = session

    async def get_all(self) -> list[{{PascalCase name}}]:
        result = await self._session.execute(select({{PascalCase name}}))
        return list(result.scalars().all())

    async def get_by_id(self, id: {{PythonPrimaryKeyType columns}}) -> {{PascalCase name}} | None:
        return await self._session.get({{PascalCase name}}, id)

    async def insert(self, entity: {{PascalCase name}}) -> {{PascalCase name}}:
        self._session.add(entity)
        await self._session.flush()
        return entity

    async def delete(self, id: {{PythonPrimaryKeyType columns}}) -> None:
        entity = await self.get_by_id(id)
        if entity is not None:
            await self._session.delete(entity)
