// Auto-generated by Daxonne — do not edit manually

import { PrismaClient } from '@prisma/client';
import { {{PascalCase name}} } from './{{PascalCase name}}';

const prisma = new PrismaClient();

export async function getAll{{PascalCase name}}(): Promise<{{PascalCase name}}[]> {
  return prisma.{{CamelCase name}}.findMany();
}

export async function get{{PascalCase name}}ById(id: {{TypeScriptPrimaryKeyType columns}}): Promise<{{PascalCase name}} | null> {
  return prisma.{{CamelCase name}}.findUnique({ where: { {{PrimaryKeyCamelCase columns}}: id } });
}

export async function create{{PascalCase name}}(data: Omit<{{PascalCase name}}, '{{PrimaryKeyCamelCase columns}}'>): Promise<{{PascalCase name}}> {
  return prisma.{{CamelCase name}}.create({ data });
}

export async function delete{{PascalCase name}}(id: {{TypeScriptPrimaryKeyType columns}}): Promise<void> {
  await prisma.{{CamelCase name}}.delete({ where: { {{PrimaryKeyCamelCase columns}}: id } });
}
